# Optional. Leave empty to use sample/manual mode without a model call.
# Create backend/.env locally. Never commit real credentials.
GEMINI_API_KEY=
# Exact image + structured-output capable model ID enabled for your account.
# Chat or AI Studio browser access alone does not verify API entitlement.
GEMINI_MODEL=
